# Runtime dependencies for the Streamlit demo deployment.
# The authoritative list is in pyproject.toml; this file exists because
# Streamlit Community Cloud installs from requirements.txt.
numpy>=1.26
scipy>=1.11
pandas>=2.0
pydantic>=2.5
pyyaml>=6.0
plotly>=5.18
streamlit>=1.32
